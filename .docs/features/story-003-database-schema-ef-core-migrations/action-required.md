# Action Required: Database Schema & EF Core Migrations

No manual steps required for this story. All tasks can be implemented automatically.

**Note:** task-03 runs `dotnet ef migrations add` which requires the EF Core CLI tools. Install if not present:
```
dotnet tool install --global dotnet-ef
```
