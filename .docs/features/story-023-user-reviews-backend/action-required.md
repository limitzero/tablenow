# Action Required: User Reviews — Backend

No manual steps required for this story.

**Note:** task-01 creates a new EF migration. Run after implementing:
```
dotnet ef migrations add AddReviews --project server/src/Migrations
dotnet ef database update --project server/src/Migrations
```
