# Action Required: 24-Hour Reminder Email

No manual steps required for this feature. All tasks can be implemented automatically.

> The reminder job reuses the email provider configured in STORY-020. Ensure `Email__ApiKey` and the verified sender are in place before testing live delivery. The EF migration is applied via the standard `dotnet ef database update` workflow.
