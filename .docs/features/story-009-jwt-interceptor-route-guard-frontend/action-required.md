# Action Required: JWT Interceptor & Route Guard — Frontend

No manual steps required for this feature. All tasks can be implemented automatically, assuming STORY-008 (the `AuthService`, login page, and `/login` route) is already in place.
