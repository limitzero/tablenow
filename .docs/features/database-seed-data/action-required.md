# Action Required: Database Seed Data

No manual steps required for this feature. All tasks can be implemented automatically.

The seed data runs at application startup — no separate CLI command is needed beyond running the app or running `dotnet ef database update` if seeding is done via `HasData`.
