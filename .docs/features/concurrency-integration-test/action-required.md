# Action Required: Concurrency Integration Test

## Before Implementation

- [ ] **Install Docker Desktop** — Testcontainers requires Docker to spin up a real SQL Server instance for the integration test. Download from https://www.docker.com/products/docker-desktop/

If Docker is not available, the test can use SQLite in-memory mode instead — see task-01-test-infra.md for the SQLite fallback configuration.

---

> This step is also referenced in task-01-test-infra.md.
