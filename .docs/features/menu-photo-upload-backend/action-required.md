# Action Required: Menu Photo Upload — Backend

## Before Implementation

- [ ] **Create uploads directory** — Create `server/uploads/` and ensure it is git-ignored. Add to `.gitignore`:
  ```
  server/uploads/
  ```

## After Implementation

- [ ] **Configure upload path for production** — Set `PhotoStorage:BasePath` environment variable to a writable directory or blob storage path.

---

> Referenced in task-02-photo-storage.md.
