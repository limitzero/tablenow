# Action Required: Database Seed Data

No manual steps required for this feature. All tasks can be implemented automatically.

> Note: Test user passwords are hashed with BCrypt and embedded in the seed. The plain-text credentials (e.g., `test@tablenow.com` / `Password123!`) should be documented in the project's developer README or `.docs/dev-setup.md` so team members know what to use during development.
