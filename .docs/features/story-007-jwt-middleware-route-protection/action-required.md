# Action Required: JWT Middleware & Route Protection — Backend

No manual steps required for this story.

**Note:** The JWT secret configured in STORY-006's action-required (`Jwt__Secret` environment variable) must be set before running the application, otherwise JWT validation will fail to start.
